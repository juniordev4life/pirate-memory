export default {
  leftMatched: state => state.leftMatched,
  highestSpeed: state => state.highestSpeed,
  elapsedMs: state => state.elapsedMs,
  amountOfCards: state => state.amountOfCards,
  cards: state => state.cards,
  status: state => state.status
}
